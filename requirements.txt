# Single source of truth for both local runs and the CI build (.github/workflows/build.yml).
fastapi
uvicorn[standard]
websockets
qrcode
pywebpush
cryptography
pillow
psutil
pywin32; platform_system == "Windows"
# build-only
pyinstaller
